{
  "name": "@joy/api",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json --noEmit false",
    "lint": "echo \"api lint pending\"",
    "test": "echo \"api test pending\"",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@joy/shared": "workspace:*",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.1",
    "tsx": "^4.19.4"
  }
}
