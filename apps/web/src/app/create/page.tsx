import { MobileShell } from '../../components/mobile-shell';

export default function CreatePage() {
  return (
    <MobileShell tab="create" tone="light">
      <main style={{ padding: '24px 16px 96px' }}>
        <h1 style={{ margin: 0, fontSize: 22 }}>发起竞猜</h1>
        <p style={{ color: '#666', lineHeight: 1.7 }}>该页面下一轮按老版本创建页重构。当前先保留导航路径。</p>
      </main>
    </MobileShell>
  );
}
