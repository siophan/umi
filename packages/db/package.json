{
  "name": "@umi/db",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "echo \"db package build pending\"",
    "lint": "echo \"db package lint pending\"",
    "test": "echo \"db package test pending\"",
    "typecheck": "tsc -p tsconfig.json"
  }
}
